    var ViewTodoItem = Truss.View.construct({        start: function () {            if (!this.options.collection) {                throw new Error("View requires a collection");            }            this.parentEl = this.options.parentEl;            this.model = this.options.model;            this.collection = this.options.collection;            this.render();            this.button.addEventListener("click", this.destroy.bind(this), false);            this.text.addEventListener("click", this.edit.bind(this), false);        },        edit: function () {            ViewTodoEdit.create({                parentEl: this.element,                model: this.model            });        },        render: function () {            var button = this.make("button"),                em = this.make("em", this.model.get("text") ),                element = this.make("li",                    [em, button], {                    id: this.model.get("id"),                    draggable: true                });            this.text = em;            this.button = button;            this.element = element;            this.parentEl.appendChild(element);        }    });