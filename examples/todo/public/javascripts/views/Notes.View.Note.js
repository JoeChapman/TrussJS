var ViewTodoItem = Truss.View.construct({    start: function () {        if (!this.collection) {            throw new Error("View requires a collection");        }        this.render();        this.addListener("click", this.destroy.bind(this), this.button);        this.addListener("click", this.edit.bind(this), this.text);    },    edit: function () {        ViewTodoEdit.create({            parentEl: this.element,            model: this.model        });    },    render: function () {        var button = this.make("button"),            em = this.make("em", this.model.get("text") ),            element = this.make("li",                [em, button], {                id: this.model.get("id"),                draggable: true            });        this.text = em;        this.button = button;        this.setElement(element);        this.parentEl.appendChild(element);    }});